# acar11x
new project use laravel 11x


# for: gitignore exclude file permissions run cmd: 
# git config core.fileMode false 


# Laravel File Manager: Lưu ý cần chạy 2 dòng sau để cấu hình css và js tệp tin có thể truy cập của thư viện.
# Nếu không sẽ báo lỗi không tải được js và css như: vendor/laravel-filemanager/js/dropzone.min.js
# 	php artisan vendor:publish --tag=lfm_config
# 	php artisan vendor:publish --tag=lfm_public